
// create empty string
// take the number and loop in the string
// add the number to the string
// 

function repeatString(number, str) {

    return str.repeat(number);
    // let newString = "";
    // for( let i = 0; i < number; i++) { 
    //     newString += str
    // }
    // return newString;
}

console.log(repeatString(4, " hello "));